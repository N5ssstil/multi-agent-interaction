# 多 Agent 交互框架

[![Python](https://img.shields.io/badge/Python-3.8%2B-blue.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

一个灵活的多 AI Agent 协作框架，支持 Agent 之间的通信、任务编排和工具调用。

## ✨ 特性

- 🤖 **多 Agent 架构** - 支持创建多个独立的 Agent 实例
- 💬 **灵活通信** - Agent 之间可以通过消息传递进行交互
- 🔄 **任务编排** - 支持顺序、并行、条件分支等任务流
- 🛠️ **工具扩展** - Agent 可以使用自定义工具完成任务
- 📊 **状态管理** - 支持共享状态和记忆系统
- 🔌 **LLM 集成** - 支持 OpenAI、Anthropic 等 LLM API

## 🚀 快速开始

### 安装

```bash
# 克隆仓库
git clone https://github.com/N5ssstil/multi-agent-interaction.git
cd multi-agent-interaction

# 安装依赖
pip install -r requirements.txt
```

### 基础示例

```python
from multi_agent import Agent, Orchestrator, MessageBus

# 创建消息总线
bus = MessageBus()

# 创建 Agent
researcher = Agent(
    name="Researcher",
    role="研究员",
    description="负责收集和整理信息",
    message_bus=bus
)

writer = Agent(
    name="Writer", 
    role="撰写者",
    description="负责撰写文章和报告",
    message_bus=bus
)

# Agent 之间通信
researcher.send_to("Writer", "我已收集到相关资料，请查收")

# 创建编排器执行任务
orchestrator = Orchestrator(agents=[researcher, writer])
result = orchestrator.run("写一篇关于 AI 的文章")
```

### 使用 LLM Agent

```python
from multi_agent import LLMAgent, ToolEnabledAgent
import os

# 创建连接真实 LLM 的 Agent
agent = LLMAgent(
    name="Assistant",
    role="AI 助手",
    model="gpt-4",
    api_key=os.getenv("OPENAI_API_KEY"),
)

# 执行任务
result = agent.execute_task("介绍一下你自己")
```

## 📁 项目结构

```
multi-agent-interaction/
├── multi_agent/           # 核心模块
│   ├── __init__.py       # 模块入口
│   ├── agent.py          # Agent 基类
│   ├── message.py        # 消息系统
│   ├── orchestrator.py   # 编排器
│   ├── tools.py          # 工具系统
│   ├── memory.py         # 记忆系统
│   └── llm_agent.py      # LLM Agent 实现
├── examples/             # 示例代码
│   ├── basic_demo.py     # 基础演示
│   ├── llm_agent_demo.py # LLM Agent 示例
│   └── openclaw_agent.py # OpenClaw 集成
├── tests/               # 测试用例
├── requirements.txt     # 依赖
└── README.md           # 说明文档
```

## 🏗️ 架构

```
┌─────────────────────────────────────────────────────────┐
│                    Orchestrator                         │
│                  (任务编排器)                            │
└─────────────────────────────────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
    ┌─────────┐       ┌─────────┐       ┌─────────┐
    │ Agent A │ ◄──►  │ Agent B │ ◄──►  │ Agent C │
    │ (专家A) │       │ (专家B) │       │ (专家C) │
    └─────────┘       └─────────┘       └─────────┘
         │                 │                 │
         └─────────────────┴─────────────────┘
                           │
                    ┌──────▼──────┐
                    │ Message Bus │
                    │  (消息总线)  │
                    └─────────────┘
```

## 🤝 Agent 类型

| 类型 | 描述 | 适用场景 |
|------|------|----------|
| **Agent** | 基础 Agent | 自定义行为 |
| **LLMAgent** | 连接 LLM API | 智能对话、分析 |
| **ToolEnabledAgent** | 支持工具调用 | 需要外部能力的任务 |

## 📝 使用场景

- 🔍 **多源信息聚合** - 多个 Agent 分别搜索不同来源
- 📊 **数据分析流水线** - 数据采集 → 清洗 → 分析 → 可视化
- 🎯 **复杂问题求解** - 专家 Agent 协作解决难题
- 🤖 **对话系统** - 多角色对话机器人
- 🔄 **工作流自动化** - 自动编排多步骤任务

## 🛠️ 开发

### 运行测试

```bash
pytest tests/ -v
```

### 运行示例

```bash
# 基础演示
python examples/basic_demo.py

# LLM Agent 演示 (需要设置 OPENAI_API_KEY)
export OPENAI_API_KEY=your-key
python examples/llm_agent_demo.py
```

## 📄 License

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'feat: add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request