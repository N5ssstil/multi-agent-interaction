# Multi-Agent Interaction Framework

多 AI Agent 交互框架 - 支持多个智能 Agent 之间的协作、通信和任务编排。

## ✨ 特性

- 🤖 **多 Agent 架构** - 支持创建多个独立的 Agent 实例
- 💬 **灵活通信** - Agent 之间可以通过消息传递进行交互
- 🔄 **任务编排** - 支持顺序、并行、条件分支等任务流
- 🛠️ **工具扩展** - Agent 可以使用自定义工具完成任务
- 📊 **状态管理** - 支持共享状态和记忆系统

## 🏗️ 架构

```
┌─────────────────────────────────────────────────────────┐
│                    Orchestrator                         │
│                  (任务编排器)                            │
└─────────────────────────────────────────────────────────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
    ┌─────────┐       ┌─────────┐       ┌─────────┐
    │ Agent A │ ◄──►  │ Agent B │ ◄──►  │ Agent C │
    │ (专家A) │       │ (专家B) │       │ (专家C) │
    └─────────┘       └─────────┘       └─────────┘
         │                 │                 │
         └─────────────────┴─────────────────┘
                           │
                    ┌──────▼──────┐
                    │ Message Bus │
                    │  (消息总线)  │
                    └─────────────┘
```

## 🚀 快速开始

### 安装

```bash
pip install -r requirements.txt
```

### 基础示例

```python
from multi_agent import Agent, Orchestrator, MessageBus

# 创建消息总线
bus = MessageBus()

# 创建 Agent
researcher = Agent(
    name="Researcher",
    role="研究员",
    description="负责收集和整理信息",
    message_bus=bus
)

writer = Agent(
    name="Writer", 
    role="撰写者",
    description="负责撰写文章和报告",
    message_bus=bus
)

# Agent 之间通信
researcher.send_to("Writer", "我已收集到相关资料，请查收")

# 创建编排器执行任务
orchestrator = Orchestrator(agents=[researcher, writer])
result = orchestrator.run("写一篇关于 AI 的文章")
```

## 📁 项目结构

```
multi-agent-interaction/
├── multi_agent/           # 核心模块
│   ├── __init__.py
│   ├── agent.py          # Agent 基类
│   ├── message.py        # 消息系统
│   ├── orchestrator.py   # 编排器
│   ├── tools.py          # 工具系统
│   └── memory.py         # 记忆系统
├── examples/             # 示例代码
├── tests/               # 测试用例
├── requirements.txt     # 依赖
└── README.md           # 说明文档
```

## 🤝 Agent 类型

| 类型 | 描述 | 适用场景 |
|------|------|----------|
| **Worker Agent** | 执行具体任务 | 数据处理、文件操作 |
| **Expert Agent** | 专业领域专家 | 问答、分析、决策 |
| **Coordinator Agent** | 协调多个 Agent | 复杂任务拆分 |
| **Observer Agent** | 监控和记录 | 日志、审计 |

## 📝 使用场景

- 🔍 **多源信息聚合** - 多个 Agent 分别搜索不同来源
- 📊 **数据分析流水线** - 数据采集 → 清洗 → 分析 → 可视化
- 🎯 **复杂问题求解** - 专家 Agent 协作解决难题
- 🤖 **对话系统** - 多角色对话机器人

## 📄 License

MIT License